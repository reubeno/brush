name: "Here docs/strings"
cases:
  - name: "Basic here doc"
    test_files:
      - path: "script.sh"
        contents: |
          cat <<END-MARKER
          Something here...
          ...and here.
          END-MARKER
    args:
      - "./script.sh"

  - name: "Basic here string"
    expected_failure: true
    stdin: |
      shopt -u posix
      cat <<< "Something here."
