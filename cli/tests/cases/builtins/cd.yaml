name: "Builtins: cd"
cases:
  - name: "Basic cd usage"
    ignore_stderr: true
    stdin: |
      cd /
      echo $?
      echo "pwd: $PWD"
      cd tmp
      echo $?
      echo "pwd: $PWD"
      cd
      echo $?
      echo "pwd: $PWD"

  - name: "cd to file"
    ignore_stderr: true
    test_files:
      - path: "file"
        contents: "file contents"
    stdin: |
      ls -1
      cd file
      echo $?
      ls -1
