name: "Options"
cases:
  - name: "Default options"
    stdin: |
      echo "Default options: $-"

  - name: "set -a"
    known_failure: true # Issue #154
    stdin: |
      unexported=original
      set -a
      newvar=bar
      unexported=updated
      env | grep newvar
      env | grep unexported
