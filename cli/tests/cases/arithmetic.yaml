name: "Arithmetic"
cases:
  - name: "Basic arithmetic"
    expected_failure: true
    stdin: |
      echo $((1+1))
      echo "$((1+1))"
      echo "$((2*3))"
      echo "$((9/3))"
      echo "$((10%3))"
      echo "$((2**3))"
