name: "Builtins: echo"
cases:
  - name: "echo with only --"
    stdin: echo --

  - name: "echo with -- and args"
    stdin: echo -- -1 --"aaa" ?^1as-

  - name: "echo -e"
    stdin: |
      echo -e "1. \x65" | hexdump -C
      echo -e "2. \x{65}" | hexdump -C

  - name: "echo options after an operand are operands"
    stdin: |
      echo hi -n
      echo a -e 'x\ty'
      echo -e a -n
      f() { echo "$@"; }
      f a -n
