name: "Builtins: help"
cases:
  - name: "Help"
    ignore_stdout: true
    ignore_stderr: true
    stdin: |
      help

  - name: "Topic-specific help"
    ignore_stdout: true
    ignore_stderr: true
    stdin: |
      help echo
