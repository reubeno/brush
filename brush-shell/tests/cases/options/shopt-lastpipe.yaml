name: "shopt lastpipe"
cases:
  - name: "shopt -s lastpipe"
    stdin: |
      echo ignored | var=value
      echo "1. var='${var}'"

      shopt -s lastpipe
      set +o monitor
      echo ignored | var=value
      echo "2. var='${var}'"
