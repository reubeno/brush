name: "Builtins: echo"
cases:
  - name: "echo with only --"
    stdin: echo --

  - name: "echo with -- and args"
    stdin: echo -- -1 --"aaa" ?^1as-

  - name: "echo -e"
    known_failure: true
    stdin: |
      echo -e "1. \x65" | hexdump -C
      echo -e "2. \x{65}" | hexdump -C
