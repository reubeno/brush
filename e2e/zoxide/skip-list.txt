# Tests that must not run at all, one pytest test name per line, each with a comment saying why.
# Reserve this for tests that hang or are irrelevant to the shell under test; anything that merely
# fails belongs in xfail-list.txt, so it keeps running and a fix is reported.
